# ── Aegis Security Platform — Python dependencies ──────────────────────────
#
# Install:   pip install -r requirements.txt
# Optional:  pip install reportlab   (enables PDF report generation)

# Web framework
flask>=3.0.0
flask-cors>=4.0.0

# HTTP client & HTML parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0          # faster BS4 parser (optional but recommended)

# Browser automation (for future Selenium-based scanning)
selenium>=4.18.0
webdriver-manager>=4.0.0

# Report generation (PDF — optional but recommended)
reportlab>=4.1.0

# Utilities
python-dotenv>=1.0.0  # for .env config files